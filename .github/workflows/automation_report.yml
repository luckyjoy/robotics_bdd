name: Robotics BDD Simulation CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
  # Runs at 1:00 AM PT every day.
  - cron: '0 8 * * *'


jobs:
  test-and-report:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    permissions:
      contents: write
      pages: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install pytest pytest-html pytest-bdd

      - name: Run tests and generate HTML report
        # The script now generates the report as index.html in the root
        run: pytest -m navigation --html=index.html --self-contained-html --capture=tee-sys

      - name: Deploy Report to GitHub Pages
        if: github.event_name == 'push' && github.ref == 'refs/heads/main' && matrix.os == 'ubuntu-latest'
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          # The publish_dir should be where your report file is located
          publish_dir: .
          publish_branch: gh-pages
          keep_files: true